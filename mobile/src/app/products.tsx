import ProductsScreen from '../screens/ProductsScreen';

export default function Products() {
  return (
    <ProductsScreen
      onAddToCart={() => {}}
      onAddToFavorites={() => {}}
    />
  );
}